server.port=${SERVER_PORT:${PORT:10000}}

spring.datasource.url=${NOTIFICATION_DATASOURCE_URL:${SPRING_DATASOURCE_URL:${JDBC_DATABASE_URL:${DATABASE_URL:${DATASOURCE_URL:jdbc:mysql://${MYSQLHOST:${MYSQL_HOST:${DB_HOST:localhost}}}:${MYSQLPORT:${MYSQL_PORT:${DB_PORT:3306}}}/${MYSQLDATABASE:${MYSQL_DATABASE:${DB_NAME:notification_db}}}?createDatabaseIfNotExist=true&sslMode=${MYSQL_SSL_MODE:${DB_SSL_MODE:REQUIRED}}&allowPublicKeyRetrieval=${MYSQL_ALLOW_PUBLIC_KEY_RETRIEVAL:${DB_ALLOW_PUBLIC_KEY_RETRIEVAL:true}}&serverTimezone=UTC}}}}}
spring.datasource.username=${NOTIFICATION_DATASOURCE_USERNAME:${SPRING_DATASOURCE_USERNAME:${DATASOURCE_USERNAME:${DATABASE_USERNAME:${MYSQLUSER:${MYSQL_USER:${DB_USER:root}}}}}}}
spring.datasource.password=${NOTIFICATION_DATASOURCE_PASSWORD:${SPRING_DATASOURCE_PASSWORD:${DATASOURCE_PASSWORD:${DATABASE_PASSWORD:${MYSQLPASSWORD:${MYSQL_PASSWORD:${DB_PASSWORD:}}}}}}}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.initialization-fail-timeout=-1
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:2}

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=${NOTIFICATION_DB_DDL_AUTO:update}
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=false
spring.jpa.show-sql=false

eureka.client.enabled=${EUREKA_CLIENT_ENABLED:false}

management.health.db.enabled=false
management.health.rabbit.enabled=${MANAGEMENT_HEALTH_RABBIT_ENABLED:false}
management.health.redis.enabled=${MANAGEMENT_HEALTH_REDIS_ENABLED:false}
